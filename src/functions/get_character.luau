--[=[
    Returns the Character associated with a `Player`

    @function GetCharacter
    @within Functions
    @param Player Player
    @return Model?
]=]
return function(Player: Player)
    return Player.Character
end
